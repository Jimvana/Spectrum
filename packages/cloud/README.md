# Spectrum Cloud

Cloud is reserved for optional hosted and team features.

The local-first open format, core, CLI, SDKs, server, dashboard, connectors, and integrations should not depend on this package.

Planned layout:

- `src/` - hosted service experiments.
- `tests/` - cloud-specific tests.
