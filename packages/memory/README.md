# Spectrum Memory

Memory owns the source-linked agent memory layer built on Spectrum packs and indexes.

Responsibilities include memory add/search/update/delete, provenance, importance/confidence metadata, deduplication, and recall APIs.

Planned layout:

- `src/` - memory implementation.
- `tests/` - memory behavior tests.
