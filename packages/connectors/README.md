# Spectrum Connectors

Connectors own ingestion from external data sources into Spectrum's common internal document format.

Each connector has its own subdirectory to keep source-specific parsing isolated.
