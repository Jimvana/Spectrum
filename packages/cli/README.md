# Spectrum CLI

CLI owns developer-facing commands such as `pack`, `search`, `decode`, `verify`, `bench`, `demo`, `inspect`, `server`, and `dashboard`.

Planned layout:

- `src/` - command implementation.
- `tests/` - command and workflow tests.
