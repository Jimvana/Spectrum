# Spectrum Dashboard

Dashboard owns the local UI for browsing packs, searching, inspecting chunks, viewing benchmarks, verifying decode fidelity, and managing memories.

Planned layout:

- `src/` - dashboard application.
